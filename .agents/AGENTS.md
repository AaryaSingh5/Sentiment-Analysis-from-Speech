# Project Rules

- **Git Commit Frequency**: Stage, commit, and push changes to GitHub at every logical step of development (e.g. after modifying a file or completing a training checkpoint verification). Do not wait until the entire task is finished to commit.
